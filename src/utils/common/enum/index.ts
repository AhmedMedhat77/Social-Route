export enum GENDER_ENUM {
  MALE = "male",
  FEMALE = "female",
}

export enum AGENT_ENUM {
  local = "local",
  google = "google",
  facebook = "facebook",
  apple = "apple",
}

export enum ROLE_ENUM {
  USER = "user",
  ADMIN = "admin",
}
